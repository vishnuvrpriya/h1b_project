
select count(job_title) as jobcount,job_title,year,(100*(count(job_title)-lag(count(job_title),1) over (order by year)) / lag(count(job_title) , 1) over (order by year)) as growth,(SUM(growth)/5) as avggrowth from h1b_final group by year order by growth desc limit 5;
